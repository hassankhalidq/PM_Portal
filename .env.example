# Injected automatically when you connect Vercel Postgres to the project.
# For local dev run: vercel env pull .env.local
POSTGRES_PRISMA_URL=""
POSTGRES_URL_NON_POOLING=""

# Generate with: openssl rand -base64 32
AUTH_SECRET=""

# Injected automatically when you connect a Vercel Blob store to the project
# (Storage tab -> Create Database -> Blob). Used for Project board attachments.
BLOB_READ_WRITE_TOKEN=""
